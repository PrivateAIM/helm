apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-po-service
  labels:
    flame-component: po
spec:
  ports:
  - name: api
    port: 8000
  selector:
    flame-component: po
    deployment-id: {{ .Release.Name }}
