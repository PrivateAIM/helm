apiVersion: v1
kind: Service
metadata:
    name: "{{ .Release.Name }}-flame-hub-server-core-worker"
spec:
    selector:
        app: "{{ .Release.Name }}-flame-hub-server-core-worker"
    ports:
        -   protocol: TCP
            port: 3000
            targetPort: 3000

