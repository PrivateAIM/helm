apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana
data:
  grafana.ini: |-
    [server]
    domain = example.com
    root_url = %(protocol)s://${HOSTNAME}/grafana
    serve_from_sub_path = false
