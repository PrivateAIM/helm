{{- if .Values.serverStorage.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: {{ .Release.Name }}-flame-hub-server-storage
    {{- with .Values.serverStorage.ingress.annotations }}
    annotations:
        {{- range $key, $value := . }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
    {{- end }}
spec:
    rules:
        - host: {{ .Values.serverStorage.ingress.hostname }}
          http:
              paths:
                  - path: {{ .Values.serverStorage.ingress.path }}
                    pathType: {{ .Values.serverStorage.ingress.pathType }}
                    backend:
                        service:
                            name: {{ .Release.Name }}-flame-hub-server-storage
                            port:
                                number: 3000
{{- end }}
