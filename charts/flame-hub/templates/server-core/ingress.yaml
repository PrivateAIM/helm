{{- if .Values.serverCore.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: {{ .Release.Name }}-flame-hub-server-core
spec:
    rules:
        - host: {{ .Values.serverCore.ingress.hostname }}
          http:
              paths:
                  - path: {{ .Values.serverCore.ingress.path }}
                    pathType: {{ .Values.serverCore.ingress.pathType }}
                    backend:
                        service:
                            name: {{ .Release.Name }}-flame-hub-server-core
                            port:
                                number: 3000
{{- end }}
