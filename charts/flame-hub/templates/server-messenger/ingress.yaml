{{- if .Values.serverMessenger.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: {{ .Release.Name }}-flame-hub-server-messenger
    {{- with .Values.serverMessenger.ingress.annotations }}
    annotations:
        {{- range $key, $value := . }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
    {{- end }}
spec:
    {{- if .Values.serverMessenger.ingress.className }}
    ingressClassName: {{ .Values.serverMessenger.ingress.className }}
    {{- end }}
    rules:
        - host: {{ .Values.serverMessenger.ingress.hostname }}
          http:
              paths:
                  - path: {{ .Values.serverMessenger.ingress.path }}
                    pathType: {{ .Values.serverMessenger.ingress.pathType }}
                    backend:
                        service:
                            name: {{ .Release.Name }}-flame-hub-server-messenger
                            port:
                                number: 3000
{{- end }}
