apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-po-service
  labels:
    component: flame-po
spec:
  ports:
  - name: api
    port: 8000
  selector:
    component: flame-po
    deployment-id: {{ .Release.Name }}
