{{- $hasCerts := include "tls.hasCerts" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-storage-deployment
  labels:
    component: flame-storage-service
    app.kubernetes.io/name: flame-storage-service
    app.kubernetes.io/component: storage
    app.kubernetes.io/part-of: flame-node
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.storageService.replicaCount }}
  selector:
    matchLabels:
      component: flame-storage-service
      app.kubernetes.io/name: flame-storage-service
      app.kubernetes.io/component: storage
      app.kubernetes.io/part-of: flame-node
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      annotations:
        {{- with .Values.storageService.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        component: flame-storage-service
        app.kubernetes.io/name: flame-storage-service
        app.kubernetes.io/component: storage
        app.kubernetes.io/part-of: flame-node
        app.kubernetes.io/version: {{ .Chart.AppVersion }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        {{- with .Values.storageService.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.storageService.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.storageService.podSecurityContext | nindent 8 }}
      containers:
        - name: storage-service
          securityContext:
            {{- toYaml .Values.storageService.securityContext | nindent 12 }}
          image: "{{ .Values.storageService.image.repository }}:{{ .Values.storageService.image.tag | default "latest" }}"
          imagePullPolicy: {{ .Values.storageService.image.pullPolicy }}
          ports:
            - containerPort: 8080
              name: api
          env:
            - name: TZ
              value: {{ .Values.timezone | default "Europe/Berlin" }}
            - name: CRYPTO__PROVIDER
              value: file
            - name:  CRYPTO__ECDH_PRIVATE_KEY_PATH
              value: /etc/crypto/private_key.pem
            - name: UVICORN_LOG_LEVEL
              value: debug
            - name: POSTGRES__HOST
              value: {{ include "postgresql.endpoint" . }}
            - name: POSTGRES__DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.user.secretName" . }}
                  key: DB_DATABASE
            - name: POSTGRES__USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.user.secretName" . }}
                  key: DB_USER
            - name: POSTGRES__PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.user.secretName" . }}
                  key: DB_PASSWORD
          {{- if or .Values.proxy.httpProxy .Values.proxy.existingSecret }}
            - name: PROXY__HTTP_URL
              {{- if .Values.proxy.existingSecret }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.proxy.existingSecret }}
                  key: httpProxy
              {{- else }}
              value: {{ .Values.proxy.httpProxy | quote }}
              {{- end }}
          {{- end }}
          {{- if or .Values.proxy.httpsProxy .Values.proxy.existingSecret }}
            - name: PROXY__HTTPS_URL
              {{- if .Values.proxy.existingSecret }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.proxy.existingSecret }}
                  key: httpsProxy
              {{- else }}
              value: {{ .Values.proxy.httpsProxy | quote }}
              {{- end }}
          {{- end }}
            - name: MINIO__ENDPOINT
              value: {{ .Release.Name }}-minio:9000
            - name: MINIO__ACCESS_KEY
              value: {{ required "MinIO user must be set." .Values.minio.rootUser | quote }}
            - name: MINIO__SECRET_KEY
              value: {{ required "MinIO password must be set." .Values.minio.rootPassword | quote }}
            - name: MINIO__USE_SSL
              value: {{ .Values.storageService.env.MINIO_USE_SSL | default false | quote }}
            - name: MINIO__BUCKET
              value: {{ required "MinIO bucket must be set." .Values.storageService.env.MINIO_BUCKET | quote }}
            - name: HUB__AUTH__FLOW
              value: "client"
            - name: HUB__AUTH__ID
              value: {{ required "Hub client ID must be set." .Values.hub.auth.clientId  | quote }}
            - name: HUB__AUTH__SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "hub.secretName" . }}
                  key: clientSecret
            - name: HUB__AUTH_BASE_URL
              value: {{ .Values.hub.endpoints.auth | default "https://auth.privateaim.dev" | quote }}
            - name: HUB__CORE_BASE_URL
              value: {{ .Values.hub.endpoints.core | default "https://core.privateaim.dev" | quote }}
            - name: HUB__STORAGE_BASE_URL
              value: {{ .Values.hub.endpoints.storage | default "https://storage.privateaim.dev" | quote }}
            - name: OIDC__CERTS_URL
              value: {{ coalesce .Values.storageService.env.OIDC_CERTS_URL (include "keycloak.jwks.endpoint" .) }}
            - name: OIDC__CLIENT_ID_CLAIM_NAME
              value: {{ .Values.storageService.env.OIDC_CLIENT_ID_CLAIM_NAME }}
            # Change this to "1" for testing purposes. This will cause the value of OIDC__CERTS_URL to be
            # ignored. You will still need to set this variable for the service to start up correctly.
            - name: OIDC__SKIP_JWT_VALIDATION
              value: "0"
            {{- if $hasCerts }}
            - name: EXTRA_CA_CERTS
              value: "/mnt/certs/certs.pem"
            {{- end }}
            - name: HUB_ADAPTER_CLIENT_ID
              value: "hub-adapter"
            {{- with .Values.storageService.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.storageService.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.storageService.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.storageService.resources | nindent 12 }}
          volumeMounts:
            - name: crypto
              mountPath: /etc/crypto
              readOnly: true
            {{- if $hasCerts }}
            - mountPath: "/mnt/certs/certs.pem"
              name: additional-certs
              subPath: "certs.pem"
              readOnly: true
            {{- end }}
            {{- with .Values.storageService.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: crypto
          secret:
            secretName: {{ include "hub.crypto.privateKeySecretName" . }}
        {{- if $hasCerts }}
        - name: additional-certs
          configMap:
            name: {{ include "tls.configMapName" . }}
        {{- end }}
        {{- with .Values.storageService.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      nodeSelector:
        {{- toYaml .Values.storageService.nodeSelector | nindent 8 }}
      affinity:
        {{- toYaml .Values.storageService.affinity | nindent 8 }}
      tolerations:
        {{- toYaml .Values.storageService.tolerations | nindent 8 }}
